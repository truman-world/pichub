# .github/workflows/deploy.yml

name: Build and Deploy to Aliyun ECS

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'
          cache: 'npm'

      # --- 关键步骤 1: 在干净的环境中安装所有依赖 ---
      - name: Install All Dependencies
        run: npm install

      - name: Build Project
        run: |
          echo "DATABASE_URL=${{ secrets.DATABASE_URL }}" > .env.local
          echo "JWT_SECRET=${{ secrets.JWT_SECRET }}" >> .env.local
          echo "NEXTAUTH_URL=${{ secrets.NEXTAUTH_URL }}" >> .env.local
          export NODE_OPTIONS=--max-old-space-size=4096 && npm run build
      
      # --- 关键步骤 2: 部署时，把 node_modules 文件夹也一起传上去 ---
      - name: Deploy to Server with node_modules
        uses: appleboy/scp-action@master
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USERNAME }}
          key: ${{ secrets.SSH_PRIVATE_KEY }} # 直接使用原始密钥
          # 我们现在把 node_modules 也加入到要上传的源文件中
          source: ".next, package.json, public, node_modules"
          target: "/www/wwwroot/8.148.7.13" 
          strip_components: 1

      - name: Restart PM2 Application
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USERNAME }}
          key: ${{ secrets.SSH_PRIVATE_KEY }} # 直接使用原始密钥
          script: |
            cd /www/wwwroot/8.148.7.13
            # 定义 pm2 的完整路径
            PM2_PATH=/www/server/nodejs/v18.20.8/bin/pm2
            # 定义 next 命令的准确相对路径
            NEXT_BIN=./node_modules/.bin/next

            # 直接用最准确的命令来启动或重载应用
            # 如果没有安装 pm2，先全局安装 (这是一个保险措施)
            npm i -g pm2
            $PM2_PATH reload pichub || $PM2_PATH start $NEXT_BIN --name "pichub" -- start
            $PM2_PATH save